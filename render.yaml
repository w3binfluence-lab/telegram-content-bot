services:
  - type: web
    name: telegram-content-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_content_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
    healthCheckPath: /
